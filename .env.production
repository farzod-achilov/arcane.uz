# ── Production environment overrides ─────────────────────
# This file is committed to git (no secrets here).
# Secrets go in .env.production.local on the server (gitignored).

NEXTAUTH_URL=https://arcane.com.uz
NEXT_PUBLIC_APP_URL=https://arcane.com.uz
NODE_ENV=production
